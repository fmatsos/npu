# Pins the exact toolchain version: rustup downloads it automatically.
# Without an explicit version, "stable" drifts and a future clippy can break
# `make qa` on unchanged code. Bump deliberately.
[toolchain]
channel = "1.98.1"
components = ["clippy", "rustfmt"]
profile = "minimal"

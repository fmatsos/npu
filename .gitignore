/target
/IMPLEMENTATION.md
/npu-cli-spec.md
.claude/settings.local.json

# The CodeGraph index: a machine-local SQLite artefact rebuilt from the
# source, not a description of it. Its wiring (.mcp.json, the hook in
# .claude/) is versioned; the index it produces is not.
/.codegraph/

# Per-developer agent tooling: each contributor picks their own, and none of
# it describes this project. The repository's own instructions live in
# CLAUDE.md, which stays versioned.
/.codex/
/.gemini/
/GEMINI.md
/opencode.jsonc
/HANDOFF.md

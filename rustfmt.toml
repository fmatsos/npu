# Deliberately near-empty: rustfmt's value comes from its defaults, which the
# whole ecosystem shares. Most "advanced" options
# (imports_granularity, group_imports, wrap_comments...) are nightly-only and
# would be ignored here.
edition = "2024"
